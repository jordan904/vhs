# Cloudflare Worker URL for the contact form (set in CI as VITE_FORM_ENDPOINT)
# After deploying the worker, this will look like:
#   https://vhs-inquiry.<your-cf-subdomain>.workers.dev
VITE_FORM_ENDPOINT=
